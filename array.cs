/*using System;
namespace Myproject
{
    public class Program
    {
        static void Main(string[] args)
        {
            string Mystring = "ghhbpcckeee";
            int Count = 0;
            foreach (char c in Mystring)
            {
                if (c == 'h')
                {
                    Count++;
                }
            }
            int k = Count;
            Console.WriteLine(k);
        }
    }
}*/
/*using System;

class GFG
{
	static int SIZE = 26;

	static void printCharWithFreq(String str)
	{
		int n = str.Length;
		int[] freq = new int[SIZE];

		for (int i = 0; i < n; i++)
			freq[str[i] - 'a']++;

		for (int i = 0; i < n; i++)
		{

			if (freq[str[i] - 'a'] != 0)
			{

				Console.Write(str[i]);
				Console.Write(freq[str[i] - 'a'] + " ");

				freq[str[i] - 'a'] = 0;
			}
		}
	}*/

	
	/*public static void Main()
	{
		String str = "ghhbpcckeee";
		printCharWithFreq(str);
	}*/
